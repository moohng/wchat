import Vue from 'vue'

let mutations = {
  // add message
  addMessage ({ sessionList }, { session, message }) {
    if (session.id) {
      // 查找会话
    } else {
      // 新建会话
      const session = {

      }
    }
  }
}

export default mutations
